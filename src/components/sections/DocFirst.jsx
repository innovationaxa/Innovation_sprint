import { Section, SectionHeader } from './_shared'

// PLACEHOLDER — to be replaced with the faithful migration from the source.
export function DocFirst() {
  return (
    <Section id="docfirst">
      <SectionHeader label="Format de référence" title="Documentation IA-first : le Markdown comme standard">
        Contenu en cours de migration.
      </SectionHeader>
    </Section>
  )
}
