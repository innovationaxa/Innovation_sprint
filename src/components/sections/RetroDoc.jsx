import { Section, SectionHeader } from './_shared'

// PLACEHOLDER — to be replaced with the faithful migration from the source.
export function RetroDoc() {
  return (
    <Section id="retrodoc" variant="alt">
      <SectionHeader label="Capacité complémentaire" title="Rétro-documentation & compréhension produit">
        Contenu en cours de migration.
      </SectionHeader>
    </Section>
  )
}
