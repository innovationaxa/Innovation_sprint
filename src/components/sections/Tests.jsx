import { Section, SectionHeader } from './_shared'

// PLACEHOLDER — to be replaced with the faithful migration from the source.
export function Tests() {
  return (
    <Section id="tests" variant="alt">
      <SectionHeader label="Module tests utilisateurs" title="Tests utilisateurs augmentés par IA">
        Contenu en cours de migration.
      </SectionHeader>
    </Section>
  )
}
